{
    "name": "cinema-plus/backend",
    "description": "cinema+ PHP Backend API",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": "^8.2 || ^8.3 || ^8.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    }
}
